<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.yexianglun-d</groupId>
    <artifactId>under-utils</artifactId>
    <version>1.0.5-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Under-Utils</name>
    <description>Java 工程模式工具包 - 封装复杂、重复、跨项目复用的基础设施与业务流程能力</description>
    <url>https://github.com/yexianglun-d/under-utils</url>
    <inceptionYear>2026</inceptionYear>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>yexianglun-d</id>
            <name>yexianglun-d</name>
            <url>https://github.com/yexianglun-d</url>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/yexianglun-d/under-utils.git</connection>
        <developerConnection>scm:git:https://github.com/yexianglun-d/under-utils.git</developerConnection>
        <url>https://github.com/yexianglun-d/under-utils</url>
        <tag>HEAD</tag>
    </scm>

    <issueManagement>
        <system>GitHub Issues</system>
        <url>https://github.com/yexianglun-d/under-utils/issues</url>
    </issueManagement>

    <ciManagement>
        <system>GitHub Actions</system>
        <url>https://github.com/yexianglun-d/under-utils/actions</url>
    </ciManagement>

    <properties>
        <!-- 编译配置 -->
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <maven.compiler.parameters>true</maven.compiler.parameters>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Maven插件版本 -->
        <maven-compiler-plugin.version>3.11.0</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.1.2</maven-surefire-plugin.version>
        <maven-source-plugin.version>3.3.0</maven-source-plugin.version>
        <maven-javadoc-plugin.version>3.5.0</maven-javadoc-plugin.version>
        <maven-deploy-plugin.version>3.1.4</maven-deploy-plugin.version>
        <maven-gpg-plugin.version>3.2.7</maven-gpg-plugin.version>
        <central-publishing-maven-plugin.version>0.10.0</central-publishing-maven-plugin.version>
        <spring-boot-maven-plugin.version>3.1.11</spring-boot-maven-plugin.version>
        <japicmp-maven-plugin.version>0.26.0</japicmp-maven-plugin.version>

        <!-- Maven Central Portal 发布配置：默认跳过上传，避免误发布。 -->
        <central.publishing.server.id>central</central.publishing.server.id>
        <central.deployment.name>${project.groupId}:${project.artifactId}:${project.version}</central.deployment.name>
        <central.autoPublish>false</central.autoPublish>
        <central.skipPublishing>true</central.skipPublishing>
        <central.waitUntil>validated</central.waitUntil>
        <api.compat.baseline.version>1.0.4</api.compat.baseline.version>
        <japicmp.skip>true</japicmp.skip>
    </properties>

    <modules>
        <!-- 依赖版本管理 BOM -->
        <module>under-utils-bom</module>
        
        <!-- 核心工具模块：字符串、日期、加密、ID 等基础工具 -->
        <module>under-utils-core</module>
        
        <!-- HTTP 请求封装：OkHttp、OpenAPI 客户端治理 -->
        <module>under-utils-http</module>

        <!-- AI 大模型基础调用封装：OpenAI-compatible 文本对话客户端 -->
        <module>under-utils-ai</module>

        <!-- Spring Boot Starter：AI 大模型基础调用客户端自动装配 -->
        <module>under-utils-ai-starter</module>

        <!-- 字段级加密与响应脱敏能力 -->
        <module>under-utils-security</module>

        <!-- Spring Boot Starter：字段级加密自动装配 -->
        <module>under-utils-security-starter</module>
        
        <!-- Redis 操作封装：分布式锁、缓存工具等 -->
        <module>under-utils-redis</module>
        
        <!-- MyBatis/MyBatis-Plus 增强：分页、元数据填充等 -->
        <module>under-utils-mybatis</module>

        <!-- Spring Boot Starter：MyBatis-Plus 默认能力自动装配 -->
        <module>under-utils-mybatis-starter</module>
        
        <!-- Spring 通用组件：统一返回体、异常处理、工具等 -->
        <module>under-utils-spring</module>

        <!-- JDBC 基础设施实现：数据库幂等状态存储等 -->
        <module>under-utils-jdbc</module>
        
        <!-- Spring Boot Starter：Spring 本地横切能力自动装配 -->
        <module>under-utils-spring-starter</module>

        <!-- Spring Boot Starter：JDBC 基础设施能力自动装配 -->
        <module>under-utils-jdbc-starter</module>

        <!-- Spring Boot Starter：Redis 分布式能力自动装配 -->
        <module>under-utils-redis-starter</module>

        <!-- Spring Boot Starter：兼容聚合入口 -->
        <module>under-utils-starter</module>
        
        <!-- 业务流程模块：导入任务、进度查询、错误导出等 -->
        <module>under-utils-biz</module>

        <!-- 示例工程：验证 starter 与工程模式封装的真实使用体验 -->
        <module>under-utils-samples</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <!-- 引入 BOM 统一版本管理 -->
            <dependency>
                <groupId>io.github.yexianglun-d</groupId>
                <artifactId>under-utils-bom</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <!-- 编译插件 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <parameters>${maven.compiler.parameters}</parameters>
                    </configuration>
                </plugin>

                <!-- 单元测试插件 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                </plugin>

                <!-- 发布插件 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>${maven-deploy-plugin.version}</version>
                </plugin>

                <!-- 源码插件 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>attach-sources</id>
                            <goals>
                                <goal>jar-no-fork</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- JavaDoc插件 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${maven-javadoc-plugin.version}</version>
                    <configuration>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <charset>${project.build.sourceEncoding}</charset>
                        <docencoding>${project.build.sourceEncoding}</docencoding>
                        <source>${java.version}</source>
                        <doclint>none</doclint>
                    </configuration>
                    <executions>
                        <execution>
                            <id>attach-javadocs</id>
                            <goals>
                                <goal>jar</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- GPG 签名插件：仅在 sign-artifacts profile 中启用 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-gpg-plugin</artifactId>
                    <version>${maven-gpg-plugin.version}</version>
                </plugin>

                <!-- Spring Boot 运行与打包插件（samples 模块使用） -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot-maven-plugin.version}</version>
                    <configuration>
                        <skip>true</skip>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>sign-artifacts</id>
            <activation>
                <property>
                    <name>gpg.sign</name>
                    <value>true</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <configuration>
                            <passphraseEnvName>MAVEN_GPG_PASSPHRASE</passphraseEnvName>
                            <gpgArguments>
                                <arg>--pinentry-mode</arg>
                                <arg>loopback</arg>
                            </gpgArguments>
                        </configuration>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>central-publish</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>${central-publishing-maven-plugin.version}</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>${central.publishing.server.id}</publishingServerId>
                            <deploymentName>${central.deployment.name}</deploymentName>
                            <autoPublish>${central.autoPublish}</autoPublish>
                            <waitUntil>${central.waitUntil}</waitUntil>
                            <skipPublishing>${central.skipPublishing}</skipPublishing>
                            <excludeArtifacts>
                                <excludeArtifact>under-utils-samples</excludeArtifact>
                            </excludeArtifacts>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>api-compat</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.github.siom79.japicmp</groupId>
                        <artifactId>japicmp-maven-plugin</artifactId>
                        <version>${japicmp-maven-plugin.version}</version>
                        <configuration>
                            <skip>${japicmp.skip}</skip>
                            <oldVersion>
                                <dependency>
                                    <groupId>${project.groupId}</groupId>
                                    <artifactId>${project.artifactId}</artifactId>
                                    <version>${api.compat.baseline.version}</version>
                                </dependency>
                            </oldVersion>
                            <newVersion>
                                <file>
                                    <path>${project.build.directory}/${project.build.finalName}.jar</path>
                                </file>
                            </newVersion>
                            <breakBuildOnBinaryIncompatibleModifications>true</breakBuildOnBinaryIncompatibleModifications>
                            <breakBuildOnSourceIncompatibleModifications>true</breakBuildOnSourceIncompatibleModifications>
                            <skipDiffReport>false</skipDiffReport>
                            <skipHtmlReport>true</skipHtmlReport>
                            <skipMarkdownReport>true</skipMarkdownReport>
                            <skipXmlReport>true</skipXmlReport>
                            <parameter>
                                <accessModifier>public</accessModifier>
                                <onlyModified>true</onlyModified>
                                <ignoreMissingClasses>true</ignoreMissingClasses>
                                <includeSynthetic>false</includeSynthetic>
                                <skipPomModules>true</skipPomModules>
                            </parameter>
                        </configuration>
                        <executions>
                            <execution>
                                <id>check-api-compatibility</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>cmp</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>integration-tests</id>
            <modules>
                <!-- 集成验证模块：通过 Testcontainers 启动外部依赖，不进入默认构建链路 -->
                <module>under-utils-test</module>
            </modules>
        </profile>
    </profiles>

</project>
